.subsubmenu
	.editfacsimiles

.row.container(class=(user.get('roleNo') >= 20)?'span7':'span5')
	.cell.span3
		.left-pane
			if entry.get('facsimiles').length
				iframe#viewer_iframe(name="viewer_iframe", scrolling="no", width="100%", frameborder="0")
	.cell.span2(style=(user.get('roleNo') < 20)?'display:none':'')
		.middle-pane
			.transcription-placeholder
				.transcription-editor
			.annotation-placeholder
				.annotation-editor
			.annotationmetadata-placeholder
				.annotationmetadata
	.cell.span2.preview-placeholder
		h2 Preview
h2 History


	| <% _.each(logEntries, function(entries, date) { %>
	h3 <%= date %>
	ul
		| <% _.each(entries, function(entry) { %>
		li
			span.username <%= entry.get('userName') %>&nbsp;
			span.comment <%= entry.get('comment') %> 
		| <% }); %>
	| <% }); %>